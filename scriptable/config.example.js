// Copy these values into Scriptable via the setup screen.
// Never commit your real CONTROL_TOKEN.

module.exports = {
  workerURL: "https://YOUR-WORKER.workers.dev",
  controlToken: "YOUR_CONTROL_TOKEN"
};
